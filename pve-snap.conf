# Default description applied to every snapshot (override per item if needed)
DEFAULT_DESC="Automated snapshot"

# Optional: set TZ explicitly (otherwise system TZ)
TZ="Europe/London"

# Items to snapshot: each line -> ID[,DESC_OVERRIDE]
# Example:
# "101,Description override"
# "204"
ITEMS=(
)
